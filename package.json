{
  "name": "cursorhack",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CursorHack notes app — sync module",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
